<!DOCTYPE html>
<html>
  <head>
    <title>FitDiary</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <!-- Подключаем Bootstrap CSS (Стили) -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 100 100%22><text y=%22.9em%22 font-size=%2290%22>💪</text></svg>">
  </head>

    <body class="d-flex flex-column min-vh-100">
    <!-- Neo Navbar -->
    <nav class="navbar navbar-expand-lg neo-navbar">
      <div class="container">
        <a class="navbar-brand fw-bold fs-3 border border-2 border-dark bg-white px-3 py-1 shadow-sm rounded-3" href="/">
          💪 FitDiary
        </a>
        
        <button class="navbar-toggler border-2 border-dark" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNav">
          <ul class="navbar-nav ms-auto gap-2"> <!-- gap-2 добавляет отступы -->
            <% if user_signed_in? %>
              <li class="nav-item"><%= link_to "Питание", food_entries_path, class: "nav-link neo-nav-link" %></li>
              <li class="nav-item"><%= link_to "Тренировки", workouts_path, class: "nav-link neo-nav-link" %></li>
              <li class="nav-item"><%= link_to "Вес", measurements_path, class: "nav-link neo-nav-link" %></li>
              
              <li class="nav-item ms-3 d-flex align-items-center">
                <%= link_to "Выйти", destroy_user_session_path, data: { turbo_method: :delete }, class: "btn btn-light border-2 border-dark fw-bold rounded-pill px-4 shadow-sm" %>
              </li>
            <% else %>
              <li class="nav-item d-flex align-items-center">
                <%= link_to "Войти", new_user_session_path, 
                  class: "btn btn-outline-dark border-2 fw-bold rounded-pill px-4 me-2" %>
              </li>
              <%= link_to "Регистрация", new_user_registration_path, 
                  class: "btn btn-light border-2 border-dark fw-bold rounded-pill px-4 shadow-sm" %>
              </li>
            <% end %>
          </ul>
        </div>
      </div>
    </nav>

    <div class="container mb-5">
      <!-- Красивые алерты -->
      <% if notice %>
        <div class="alert bg-white border-2 border-dark rounded-3 shadow-sm fw-bold mb-4" role="alert">
          ✨ <%= notice %>
        </div>
      <% end %>

      <%= yield %>
    </div>
  </body>

</html>
