<div class="mb-4">
  <%= link_to "← Все тренировки", workouts_path, class: "fw-bold text-dark text-decoration-none" %>
</div>

<div class="row">
  <div class="col-lg-8">
    
    <!-- Заголовок тренировки -->
    <div class="card p-4 mb-4 bg-warning">
      <h1 class="display-6 fw-bold mb-0"><%= @workout.title %></h1>
      <p class="fw-bold opacity-75 mb-0">Дата: <%= @workout.date_performed.strftime("%d.%m.%Y") %></p>
    </div>

    <!-- Здесь должен быть список упражнений (WorkoutItems) -->
    <!-- Так как мы пока не настраивали детальный вывод упражнений, сделаем заглушку в стиле -->
    
    <div class="card p-5 text-center dashed-border" style="border-style: dashed !important;">
      <h3 class="text-muted">Список упражнений</h3>
      <p>Здесь будут подходы, повторения и веса.</p>
      
      <!-- Это место для будущего функционала "Добавить упражнение" -->
      <button class="btn btn-secondary disabled">Функционал в разработке</button>
    </div>

  </div>

  <div class="col-lg-4">
    <div class="card p-3">
      <h5 class="fw-bold">Действия</h5>
      <%= link_to "Редактировать название", edit_workout_path(@workout), class: "btn btn-outline-secondary w-100 mb-2" %>
      <%= link_to "Удалить тренировку", @workout, data: { turbo_method: :delete, turbo_confirm: "Точно?" }, class: "btn btn-danger w-100 border-2 border-dark" %>
    </div>
  </div>
</div>
