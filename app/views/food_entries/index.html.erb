<div class="d-flex justify-content-between align-items-center mb-4">
  <h1 class="display-5">Дневник питания</h1>
  <%= link_to "Добавить запись", new_food_entry_path, class: "btn-neo-primary" %>
</div>

    <div class="card p-0 overflow-hidden">
      <table class="neo-table mb-0" style="width: 100%;">
        <thead>
          <tr>
            <th>Дата</th>
            <th>Прием</th>
            <th>Продукт</th>
            <th>Вес</th>
            <th>Ккал</th>
            <th class="text-end">Действия</th>
          </tr>
        </thead>
        <tbody>
          <% @food_entries.each do |food_entry| %>
            <tr>
              <!-- 1. Дата -->
              <td class="align-middle"><%= food_entry.date_eaten&.strftime("%d.%m") %></td>
              
              <!-- 2. Прием пищи -->
              <td class="align-middle">
                <% if food_entry.meal_type %>
                  <span class="badge bg-warning text-dark border border-dark rounded-pill">
                    <%= food_entry.meal_type.name %>
                  </span>
                <% end %>
              </td>

              <!-- 3. Продукт -->
              <td class="fw-bold align-middle">
                <%= food_entry.product&.name || "Удален" %>
              </td>

              <!-- 4. Вес -->
              <td class="align-middle"><%= food_entry.weight_grams %> г</td>

              <!-- 5. Ккал -->
              <td class="align-middle fw-bold">
                <% if food_entry.product %>
                  <%= ((food_entry.product.calories || 0) * (food_entry.weight_grams || 0) / 100.0).round(0) %>
                <% else %>
                  0
                <% end %>
              </td>
              
              <!-- 6. КНОПКИ -->
              <td class="text-end align-middle">
                 <div class="d-flex justify-content-end gap-2">
                   <%= link_to "👁️", food_entry, class: "btn btn-sm btn-outline-secondary border-2 border-dark fw-bold", title: "Показать" %>
                   <%= link_to "✏️", edit_food_entry_path(food_entry), class: "btn btn-sm btn-light border-2 border-dark fw-bold", title: "Редактировать" %>
                 </div>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>

