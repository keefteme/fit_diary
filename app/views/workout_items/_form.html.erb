<%= form_with(model: workout_item) do |form| %>
  <% if workout_item.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(workout_item.errors.count, "error") %> prohibited this workout_item from being saved:</h2>

      <ul>
        <% workout_item.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :comment, style: "display: block" %>
    <%= form.text_field :comment %>
  </div>

  <div>
    <%= form.label :workout_id, style: "display: block" %>
    <%= form.text_field :workout_id %>
  </div>

  <div>
    <%= form.label :exercise_id, style: "display: block" %>
    <%= form.text_field :exercise_id %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
